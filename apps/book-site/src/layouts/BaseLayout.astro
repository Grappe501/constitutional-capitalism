---
import "../styles/global.css";
import ListenBar from "../components/ListenBar.astro";
import { identity } from "../lib/data";
import siteNav from "../../../../data/project/site_spine_navigation.json";

interface Props {
  title?: string;
  description?: string;
  path?: string;
  bareMain?: boolean;
  /** Strip engineering chrome — book title, prose, prev/next. */
  readingRoom?: boolean;
}

const {
  title,
  description = "Constitutional Capitalism — Restoring Prosperity Through Constitutional Principles. A free public book project in development.",
  path = "/",
  bareMain = false,
  readingRoom = false,
} = Astro.props;

const pageTitle = title
  ? `${title} | ${identity.title}`
  : `${identity.title} | ${identity.subtitle}`;
const canonical = new URL(path, Astro.site).toString();
const primary = siteNav.primary_nav || [];
const readingPrimary = [
  { href: "/book/", label: "Book" },
  { href: "/public-book/table-of-contents/", label: "Contents" },
];
const headerPrimary = readingRoom ? readingPrimary : primary;
const foundations = siteNav.foundations;
const spines = siteNav.spines || [];
const researchDesk = siteNav.research_desk;
const buildProgress = siteNav.build_progress;
const footerColumns = siteNav.footer_columns || [];

const readerChrome =
  path === "/book/" ||
  path.startsWith("/public-book/") ||
  path.startsWith("/read/");

const bodyChrome = [
  readerChrome ? "chrome-reader" : "",
  readingRoom ? "chrome-reading-room" : "",
]
  .filter(Boolean)
  .join(" ") || undefined;

function navIsCurrent(href: string, currentPath: string) {
  if (href === "/book/") {
    return (
      currentPath === "/book/" ||
      currentPath.startsWith("/public-book/") ||
      currentPath.startsWith("/read/")
    );
  }
  return currentPath === href || currentPath.startsWith(href);
}

function chipClass(status?: string) {
  if (status === "COMPLETE") return "chip chip--done";
  if (status === "ACTIVE") return "chip chip--live";
  return "chip chip--queued";
}

function statusLabel(status?: string) {
  if (status === "COMPLETE") return "Pass complete";
  if (status === "ACTIVE") return "Building now";
  return "Queued";
}
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <meta name="robots" content="index,follow" />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:site_name" content={identity.title} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={description} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <slot name="head" />
  </head>
  <body class={bodyChrome}>
    <a class="skip-link" href="#main">Skip to content</a>
    <header class="site-header" data-site-header>
      <div class="site-header__bar">
        <a class="brand" href={readingRoom ? "/book/" : "/"}>
          <span class="brand__mark" aria-hidden="true"></span>
          <span class="brand__full">{identity.title}</span>
          <span class="brand__short" aria-hidden="true">CC</span>
        </a>
        <div class="header-actions">
          <nav class="primary-links" aria-label="Primary">
            {
              headerPrimary.map((item) => (
                <a
                  href={item.href}
                  aria-current={navIsCurrent(item.href, path) ? "page" : undefined}
                >
                  {item.label}
                </a>
              ))
            }
          </nav>
          <button
            type="button"
            class="nav-toggle"
            data-nav-toggle
            aria-expanded="false"
            aria-controls="site-nav-panel"
          >
            <span data-nav-toggle-label>{readingRoom ? "Menu" : "More"}</span>
          </button>
        </div>
      </div>
      <div class="nav-panel" id="site-nav-panel" data-nav-panel>
        <div class="nav-panel__inner">
          {
            readingRoom ? (
              <>
                <section class="nav-group" aria-labelledby="nav-read">
                  <h2 id="nav-read">Chapters</h2>
                  <ul class="nav-group__links">
                    <li>
                      <a href="/public-book/the-arkansas-we-inherited/">
                        1 — The Arkansas We Inherited
                      </a>
                    </li>
                    <li>
                      <a href="/public-book/what-does-prosperity-mean/">
                        2 — What Does Prosperity Mean?
                      </a>
                    </li>
                    <li>
                      <a href="/public-book/follow-the-money/">3 — Follow the Money</a>
                    </li>
                    <li>
                      <a href="/public-book/people-are-the-real-capital/">
                        4 — People Are the Real Capital
                      </a>
                    </li>
                    <li>
                      <a href="/public-book/the-productive-arkansas/">
                        5 — The Productive Arkansas
                      </a>
                    </li>
                    <li>
                      <a href="/public-book/government-that-belongs-to-the-people/">
                        6 — Government That Belongs to the People
                      </a>
                    </li>
                    <li>
                      <a href="/public-book/so-what-is-constitutional-capitalism/">
                        7 — So What Is Constitutional Capitalism?
                      </a>
                    </li>
                    <li>
                      <a href="/public-book/building-the-bridge/">8 — Building the Bridge</a>
                    </li>
                    <li>
                      <a href="/public-book/we-tried-to-break-it/">9 — We Tried to Break It</a>
                    </li>
                    <li>
                      <a href="/public-book/the-arkansas-we-could-leave-behind/">
                        10 — The Arkansas We Could Leave Behind
                      </a>
                    </li>
                    <li>
                      <a href="/public-book/table-of-contents/">Full table of contents</a>
                    </li>
                  </ul>
                </section>
                <section class="nav-group" aria-labelledby="nav-deeper">
                  <h2 id="nav-deeper">Go deeper</h2>
                  <p>Optional. The book stands alone.</p>
                  <ul class="nav-group__links">
                    <li>
                      <a href="/evidence/">Sources / Evidence</a>
                    </li>
                    <li>
                      <a href="/book/">All chapters</a>
                    </li>
                    <li>
                      <a href="/">Project home</a>
                    </li>
                  </ul>
                </section>
              </>
            ) : (
              <>
          <section class="nav-group" aria-labelledby="nav-read">
            <h2 id="nav-read">Read the book</h2>
            <p>Volume II is the front door. Everything else is drill-down.</p>
            <ul class="nav-group__links">
              <li><a href="/book/">Start reading</a></li>
              <li><a href="/public-book/the-arkansas-we-inherited/">Chapter 1 — The Arkansas We Inherited</a></li>
              <li><a href="/public-book/introduction/">Introduction</a></li>
              <li><a href="/public-book/readers-contract/">Reader's Contract</a></li>
              <li><a href="/public-book/table-of-contents/">Table of contents</a></li>
              <li><a href="/public-book/">Edition notes &amp; tools</a></li>
            </ul>
          </section>

          <section class="nav-group" aria-labelledby="nav-deeper">
            <h2 id="nav-deeper">Go deeper</h2>
            <p>Learn, prove, and build — available when you want them.</p>
            <ul class="nav-group__links">
              <li><a href="/compare/explorer/">Comparison Explorer</a></li>
              <li><a href="/unabridged/">Unabridged</a></li>
              <li><a href="/evidence/">Evidence</a></li>
              <li><a href="/v2/">V2 Blueprint</a></li>
              <li><a href="/system/">System spines</a></li>
            </ul>
          </section>

          <section class="nav-group" aria-labelledby="nav-spines">
            <h2 id="nav-spines">System spines</h2>
            <p>Architecture by lane — for readers who want the machinery behind the book.</p>
            <div class="nav-spine-row">
              {
                spines.map((spine) => (
                  <a class="nav-spine-card" href={spine.href}>
                    <span class={chipClass(spine.status)}>{statusLabel(spine.status)}</span>
                    <strong>{spine.title}</strong>
                    <span>{spine.blurb}</span>
                  </a>
                ))
              }
            </div>
          </section>

          <section class="nav-group" aria-labelledby="nav-foundations">
            <h2 id="nav-foundations">{foundations.title}</h2>
            <p>{foundations.blurb}</p>
            <ul class="nav-group__links">
              {
                foundations.links.map((link) => (
                  <li>
                    <a href={link.href}>{link.label}</a>
                  </li>
                ))
              }
            </ul>
          </section>

          <section class="nav-group" aria-labelledby="nav-build">
            <h2 id="nav-build">{buildProgress?.title || "Build Progress"}</h2>
            <p>{buildProgress?.blurb}</p>
            <ul class="nav-group__links">
              <li><a href="/build-progress/">Build Progress hub</a></li>
              <li><a href="/build-progress/passes/">All passes</a></li>
              {
                (buildProgress?.passes || []).map((link) => (
                  <li>
                    <a href={link.href}>{link.label}</a>
                  </li>
                ))
              }
              <li><a href="/status/">Status dials</a></li>
              <li><a href="/system/arkansas-state-baseline/">Arkansas State Baseline</a></li>
            </ul>
          </section>

          <section class="nav-group" aria-labelledby="nav-research">
            <h2 id="nav-research">Research desk</h2>
            <p>{researchDesk.blurb}</p>
            <ul class="nav-group__links">
              <li><a href="/research/">Open research desk</a></li>
              {
                researchDesk.working_now.map((link) => (
                  <li>
                    <a href={link.href}>{link.label}</a>
                  </li>
                ))
              }
              {
                researchDesk.evidence_surfaces.map((link) => (
                  <li>
                    <a href={link.href}>{link.label}</a>
                  </li>
                ))
              }
            </ul>
          </section>
              </>
            )
          }
        </div>
      </div>
    </header>
    <main id="main" class={bareMain ? "home-main" : "shell"} data-listen-root>
      <slot />
    </main>
    <ListenBar />
    <footer class="site-footer">
      <div class="site-footer__inner">
        {
          readingRoom ? (
            <>
              <p>
                <strong>{identity.title}</strong>
              </p>
              <p>
                <a href="/book/">All chapters</a>
                {" · "}
                <a href="/evidence/">Sources</a>
                {" · "}
                <a href="/">Project home</a>
              </p>
            </>
          ) : (
            <>
              <p>
                <strong>{identity.title}</strong> — {identity.subtitle}
              </p>
              <div class="footer-grid">
                {
                  footerColumns.map((col) => (
                    <div>
                      <h2>{col.title}</h2>
                      <ul>
                        {col.links.map((link) => (
                          <li>
                            <a href={link.href}>{link.label}</a>
                          </li>
                        ))}
                      </ul>
                    </div>
                  ))
                }
              </div>
              <p>
                Free public edition · Volume II ready to read · Licensing decision open ·
                Project status: {identity.project_status}.
              </p>
            </>
          )
        }
      </div>
    </footer>
    <script define:vars={{ readingRoomMenuLabel: readingRoom ? "Menu" : "More" }}>
      const header = document.querySelector("[data-site-header]");
      const toggle = document.querySelector("[data-nav-toggle]");
      const panel = document.querySelector("[data-nav-panel]");
      const label = document.querySelector("[data-nav-toggle-label]");
      if (header && toggle && panel && label) {
        const setOpen = (open: boolean) => {
          header.classList.toggle("is-nav-open", open);
          document.body.classList.toggle("nav-lock", open);
          toggle.setAttribute("aria-expanded", open ? "true" : "false");
          label.textContent = open ? "Close" : readingRoomMenuLabel;
        };
        toggle.addEventListener("click", () => {
          setOpen(!header.classList.contains("is-nav-open"));
        });
        panel.querySelectorAll("a").forEach((link) => {
          link.addEventListener("click", () => setOpen(false));
        });
        document.addEventListener("keydown", (event) => {
          if (event.key === "Escape") setOpen(false);
        });
      }
    </script>
  </body>
</html>
