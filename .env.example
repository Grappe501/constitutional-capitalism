# Constitutional Capitalism — local secrets (never commit real values)
# Copy to .env (gitignored) or set in your shell / Netlify board site env.

# OpenAI — server/CLI / Netlify Functions only. NEVER put in browser or public assets.
OPENAI_API_KEY=
# Optional model override for CC-AI-RESEARCH-INTELLIGENCE-LAYER-1.0
OPENAI_MODEL=gpt-4o-mini
